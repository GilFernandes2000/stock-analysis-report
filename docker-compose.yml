services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - FRONTEND_DIST_PATH=/frontend/dist
      - SERVE_FRONTEND=true
    volumes:
      - app-data:/app/data
    working_dir: /app

volumes:
  app-data:
